# AI大模型学习知识图谱 - 启动说明

## 项目简介

这是一个基于 React + Vite + ECharts 构建的交互式AI大模型学习知识图谱系统，将原有的HTML知识图谱重构为现代化的npm项目。

## 环境要求

- Node.js >= 16.0.0
- npm >= 8.0.0 或 yarn >= 1.22.0

## 安装步骤

### 1. 安装依赖

在项目根目录(`ai-study`)下执行:

```bash
npm install
```

或使用 yarn:

```bash
yarn install
```

### 2. 启动开发服务器

```bash
npm run dev
```

或:

```bash
yarn dev
```

启动后会自动在浏览器中打开 `http://localhost:3000`

### 3. 构建生产版本

```bash
npm run build
```

构建完成后，产物在 `dist` 目录下。

### 4. 预览生产构建

```bash
npm run preview
```

## 项目结构

```
ai-study/
├── src/
│   ├── components/          # React组件
│   │   ├── KnowledgeGraph.jsx    # 知识图谱主组件
│   │   ├── Sidebar.jsx           # 侧边栏组件
│   │   ├── ControlBar.jsx        # 控制栏组件
│   │   └── TitleBar.jsx          # 标题栏组件
│   ├── data/               # 数据文件
│   │   ├── hierarchy.js          # 知识层级数据
│   │   └── descriptions.js       # 节点描述
│   ├── utils/              # 工具函数
│   │   └── colorUtils.js         # 颜色工具
│   ├── styles/             # 样式文件
│   │   └── index.css
│   ├── App.jsx             # 主应用组件
│   └── main.jsx            # 入口文件
├── public/                 # 静态资源
├── index.html              # HTML模板
├── package.json            # 项目配置
├── vite.config.js          # Vite配置
└── README.md               # 项目文档
```

## 功能特性

✅ **交互式知识图谱**: 基于 ECharts 的径向树状图展示
✅ **多层级导航**: 支持从总览到具体技术的多层级浏览
✅ **响应式设计**: 适配桌面和移动设备
✅ **侧边栏详情**: 点击节点查看详细信息和子节点
✅ **控制功能**: 支持重新布局、展开全部、折叠全部
✅ **美观UI**: 现代化的深色主题设计

## 知识体系覆盖

1. **AI基础理论** - 神经网络架构、深度学习基础
2. **数据工程** - 数据收集、处理、增强、质量保证
3. **大模型核心技术** - 微调、量化、训练、推理、强化学习
4. **应用开发与框架** - RAG、Agent、向量数据库、HuggingFace工具链
5. **评估与安全** - 模型评测、安全防御
6. **部署与运维** - LLMOps、性能分析
7. **AI基础设施** - 硬件集群、AI编译器、国产化适配

## 常见问题

### 端口被占用

如果3000端口被占用，可以修改 `vite.config.js` 中的端口配置:

```javascript
server: {
  port: 3001, // 修改为其他端口
  open: true
}
```

### 依赖安装失败

尝试清除缓存后重新安装:

```bash
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### 构建失败

确保 Node.js 版本 >= 16，可以使用 nvm 切换版本:

```bash
nvm use 16
```

## 技术支持

如有问题，请查看:
- [Vite 文档](https://vitejs.dev/)
- [React 文档](https://react.dev/)
- [ECharts 文档](https://echarts.apache.org/)

## 开发建议

- 修改知识图谱数据: 编辑 `src/data/hierarchy.js`
- 调整颜色方案: 编辑 `src/utils/colorUtils.js`
- 修改样式: 编辑 `src/styles/index.css`
- 调整图表配置: 编辑 `src/components/KnowledgeGraph.jsx`

## 下一步优化

- [ ] 添加搜索功能
- [ ] 支持导出图片
- [ ] 添加更多交互动画
- [ ] 集成Markdown渲染
- [ ] 添加暗色/亮色主题切换
- [ ] 支持多语言

---

祝学习愉快! 🚀
